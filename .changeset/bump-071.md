---
'prest-js-sdk': patch
---

Bump to 0.7.1 — 0.7.0 was published then unpublished earlier today, and
npm does not allow reusing an unpublished version number. No code
changes in this release; it re-ships the auto-camelCase feature from
the 0.7.0 attempt under a fresh version tag.
